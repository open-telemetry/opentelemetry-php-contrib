file_format: '0.4'
instrumentation/development: &config
  php:
    http:
      uri_sanitizers:
        - default:
        - redact_query_string_values:
            query_keys: [ passwd, secret ]

instrumentation: *config # OpenTelemetry uses outdated 'instrumentation' key
