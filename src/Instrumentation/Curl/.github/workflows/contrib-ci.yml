name: Contrib CI Pipeline
on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  CI:
    uses: open-telemetry/opentelemetry-php-contrib/.github/workflows/instrumentation-curl.yml@302f221b8909a540ac467152a0e21b9e428adf29 # main
    with:
      package-root: '.'
