file_format: '1.0-rc.2'

logger_provider:
  processors:
    - simple:
        exporter:
          test/in_memory_exporter: {}

tracer_provider:
  processors:
    - simple:
        exporter:
         test/in_memory_exporter: {}

instrumentation/development:
  php:
    laravel:
      enabled: true
