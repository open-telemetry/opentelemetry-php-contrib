file_format: '0.4'

logger_provider:
  processors:
    - simple:
        exporter:
          test/in_memory_exporter: {}

tracer_provider:
  processors:
    - simple:
        exporter:
         test/in_memory_exporter: {}

instrumentation/development:
  php:
    laravel:
      enabled: true
