file_format: '0.3'

logger_provider:
  processors:
    - simple:
        exporter:
          test/in_memory_exporter: {}

tracer_provider:
  processors:
    - simple:
        exporter:
         test/in_memory_exporter: {}

instrumentation:
  php:
    laravel:
      enabled: true
